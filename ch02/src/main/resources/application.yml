server:
  port: 9092

# 로깅
logging:
  level:
    root: info
    org.springframework.boot.web: debug

spring:
  # jconsole admin
  application:
    admin:
      enabled: true
  # Live Reload
  devtools:
    livereload:
      enabled: true


service:
  message:
    text: "hello"